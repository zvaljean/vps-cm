#+title: Dot Config

* environment
- fish
#+begin_src conf :tangle (concat local-dir "basic-env/env.sh")  :mkdirp yes

 set -gx  PAGER 'less'
 set -gx  LC_TIME en_US.UTF-8
 set -gx  LC_ALL en_US.UTF-8
 set -gx  LC_CTYPE UTF-8
 set -gx  GNUPGHOME ~/.gnupg
 set -gx  GPG_TTY $(tty)
 set -gx  EDITOR 'nvim'
 set -gx  VISUAL 'vim'
 set -gx  LANG en_US.UTF-8
 set -gx  NIX_REMOTE daemon

#+end_src

* ssh 
** user config
*** source data
#+name: gen-user-config
#+header: :var data=(expand-file-name "~/.ssh/conf.d/vps/vps.conf")
#+begin_src sh :results output
    sed -n '/^####data-user/,/^####data-user/{//!p}'  "$data"  | chezmoi encrypt
#+end_src
*** exec
#+begin_src conf :tangle (concat ssh-conf-dir "encrypted_private_config") :noweb yes  :mkdirp yes
   <<gen-user-config()>>
#+end_src
* chezmoi
#+begin_src conf :tangle (concat cm-config  "chezmoi.toml.tmpl") :mkdirp yes
format = "json"
encryption = "gpg"
[gpg]
recipient = "7E7C703D57522B91"
[pinentry]
command = "/usr/bin/pinentry-tty"
[edit]
command = "nvim"
#+end_src


* local variable
# Local Variables:
# local-dir: "../chezmoi/dot_local/config/"
# ssh-conf-dir: "../chezmoi/dot_ssh/"
# cm-config: "../chezmoi/dot_config/chezmoi/"
# eval: (add-hook 'after-save-hook #'org-babel-tangle nil t)
# org-confirm-babel-evaluate: nil
# End:
